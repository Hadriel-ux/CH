# QR Web Server

This project sets up a simple web server using Express and the QRCode package to serve QR codes. 

## Installation

```bash
npm install express qrcode
```

## Usage

Run the server:
```bash
node server.js
```